{% extends 'layouts/base.twig' %}

{% block title %}{{ post.title }} — {{ site_name|default('CMS-Flexi') }}{% endblock %}

{% block breadcrumb %}
<div class="container">
    <div class="headline bg0 flex-wr-sb-c p-rl-20 p-tb-8">
        <div class="f2-s-1 p-r-30 m-tb-6">
            <a href="/" class="breadcrumb-item f1-s-3 cl9">Главная</a>
            <a href="/category/{{ post.category_slug|default('news') }}" class="breadcrumb-item f1-s-3 cl9">
                {{ post.category|default('Новости') }}
            </a>
            <span class="breadcrumb-item f1-s-3 cl9">{{ post.title }}</span>
        </div>
    </div>
</div>
{% endblock %}

{% block content %}
<section class="bg0 p-b-140 p-t-10">
    <div class="container">
        <div class="row justify-content-center">

            <div class="col-md-10 col-lg-8 p-b-30">
                <article class="p-r-10 p-r-0-sr991">
                    <div class="p-b-70">
                        <a href="/category/{{ post.category_slug|default('news') }}"
                           class="f1-s-10 cl2 hov-cl10 trans-03 text-uppercase">
                            {{ post.category|default('Новости') }}
                        </a>

                        <h3 class="f1-l-3 cl2 p-b-16 p-t-33 respon2">{{ post.title }}</h3>

                        <div class="flex-wr-s-s p-b-40">
                            <span class="f1-s-3 cl8 m-r-15">
                                <a href="#" class="f1-s-4 cl8 hov-cl10 trans-03">by {{ post.author|default('Anonymous') }}</a>
                                <span class="m-rl-3">-</span>
                                <span>{{ post.date }}</span>
                            </span>

                            <span class="f1-s-3 cl8 m-r-15">{{ post.views|default(0) }} просмотров</span>

                            <a href="#comments" class="f1-s-3 cl8 hov-cl10 trans-03 m-r-15">
                                {{ (post.comments_count|default(0)) }} комментариев
                            </a>
                        </div>

                        {% if post.image is defined %}
                            <div class="wrap-pic-max-w p-b-30">
                                <img src="{{ post.image }}" alt="{{ post.title }}">
                            </div>
                        {% endif %}

                        <div class="f1-s-11 cl6 p-b-25">
                            {{ post.content|raw }}
                        </div>

                        {% if post.tags is defined and post.tags|length %}
                            <div class="flex-wr-s-s p-t-15">
                                <span class="f1-s-12 cl3 m-r-12">Теги:</span>
                                {% for tag in post.tags %}
                                    <a href="/tag/{{ tag }}"
                                       class="f1-s-1 cl8 hov-cl10 trans-03 m-r-10">#{{ tag }}</a>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                </article>
            </div>

            <div class="col-md-10 col-lg-4 p-b-30">
                {% include 'layouts/sidebar.twig' %}
            </div>

        </div>
    </div>
</section>
{% endblock %}
